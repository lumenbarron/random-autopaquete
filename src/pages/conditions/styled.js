import styled from 'styled-components';

export const StyledConditions = styled.div`
    .container {
        position: relative;
        width: 100%;
        min-height: 100vh;
        aling-items: left;
        padding: 0 250px;
        margin: 0;
    }
`;
